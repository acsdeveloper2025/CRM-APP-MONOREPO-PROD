                                         Table "public.pincodes"
  Column   |           Type           | Collation | Nullable |                  Default                  
-----------+--------------------------+-----------+----------+-------------------------------------------
 code      | character varying(10)    |           | not null | 
 createdAt | timestamp with time zone |           |          | CURRENT_TIMESTAMP
 updatedAt | timestamp with time zone |           |          | CURRENT_TIMESTAMP
 id        | integer                  |           | not null | nextval('pincodes_temp_id_seq'::regclass)
 cityId    | integer                  |           |          | 
Indexes:
    "pincodes_pkey" PRIMARY KEY, btree (id)
    "idx_pincodes_city_id" btree ("cityId")
    "idx_pincodes_code" btree (code)
    "pincodes_code_key" UNIQUE CONSTRAINT, btree (code)
Foreign-key constraints:
    "fk_pincodes_city" FOREIGN KEY ("cityId") REFERENCES cities(id)
Referenced by:
    TABLE ""pincodeAreas"" CONSTRAINT "fk_pincode_areas_pincode" FOREIGN KEY ("pincodeId") REFERENCES pincodes(id)
    TABLE ""userAreaAssignments"" CONSTRAINT "fk_user_area_assignments_pincode" FOREIGN KEY ("pincodeId") REFERENCES pincodes(id) ON DELETE CASCADE
    TABLE ""userPincodeAssignments"" CONSTRAINT "fk_user_pincode_assignments_pincode" FOREIGN KEY ("pincodeId") REFERENCES pincodes(id) ON DELETE CASCADE
    TABLE "verifications" CONSTRAINT "fk_verifications_pincode" FOREIGN KEY (pincode_id) REFERENCES pincodes(id)


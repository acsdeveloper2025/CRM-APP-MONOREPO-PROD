                                          Table "public.roles"
    Column    |           Type           | Collation | Nullable |                Default                 
--------------+--------------------------+-----------+----------+----------------------------------------
 name         | character varying(100)   |           | not null | 
 description  | text                     |           |          | 
 permissions  | jsonb                    |           | not null | '{}'::jsonb
 isSystemRole | boolean                  |           | not null | false
 isActive     | boolean                  |           | not null | true
 createdAt    | timestamp with time zone |           |          | CURRENT_TIMESTAMP
 updatedAt    | timestamp with time zone |           |          | CURRENT_TIMESTAMP
 createdBy    | uuid                     |           |          | 
 updatedBy    | uuid                     |           |          | 
 id           | integer                  |           | not null | nextval('roles_temp_id_seq'::regclass)
Indexes:
    "roles_pkey" PRIMARY KEY, btree (id)
    "idx_roles_is_active" btree ("isActive")
    "idx_roles_is_system_role" btree ("isSystemRole")
    "idx_roles_name" btree (name)
    "idx_roles_permissions" gin (permissions)
    "roles_name_key" UNIQUE CONSTRAINT, btree (name)
Foreign-key constraints:
    "fk_roles_created_by" FOREIGN KEY ("createdBy") REFERENCES users(id) ON DELETE SET NULL
    "fk_roles_updated_by" FOREIGN KEY ("updatedBy") REFERENCES users(id) ON DELETE SET NULL
Referenced by:
    TABLE "users" CONSTRAINT "fk_users_role_id" FOREIGN KEY ("roleId") REFERENCES roles(id)
Triggers:
    update_roles_updated_at BEFORE UPDATE ON roles FOR EACH ROW EXECUTE FUNCTION update_roles_updated_at()

